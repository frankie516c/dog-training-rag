# 수집 데이터는 저장소에 올리지 않는다.
# 자막/오디오는 용량 문제도 있지만, 무엇보다 원저작물이라 배포 대상이 아니다.
data/*
!data/reviews/
data/reviews/*
!data/reviews/bodeum_youtube_manual_reviews.csv

# 평가 질문셋은 수집물이 아니라 사람이 작성·검토하는 산출물이라 추적한다.
# 생성물(results/, review/, cache/)은 계속 제외한다.
!data/eval/
data/eval/*
!data/eval/queries/
# queries/ 안은 전부 추적한다. 질문셋 본체뿐 아니라 합성 질문을 만든 프롬프트와
# 매핑·원본 응답(_synthetic_*)까지 있어야 평가셋을 다시 만들 수 있다.
# 재생성 캐시는 이 디렉터리에 두지 않는다.
!data/eval/queries/**
*.vtt
*.srt
*.wav
*.m4a
*.mp3

# 벡터 인덱스 / 임베딩 캐시
index/
*.faiss
*.sqlite3
*.duckdb

# 파이썬
.venv/
venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# 환경 변수 (API 키)
.env
.env.*
!.env.example

# 에디터 / OS
.vscode/
.idea/
Thumbs.db
desktop.ini

!data/eval/results/
data/eval/results/*
!data/eval/results/baseline_*
!data/eval/results/exp_a*