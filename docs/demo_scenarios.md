# 데모 시나리오 매핑

네 개 시나리오가 각각 어느 질문으로 도는지, 그 질문이 마지막 측정에서 어떻게 나왔는지.

| 시나리오 | 질문 | 출처 | 기록 위치 |
|---|---|---|---|
| ① 단일 홉 · 잘 되는 경우 | `q007` **(조달 후 유지 확정)** | gold 세트 | **이 문서** (아래 사유) |
| ① 대기 | `q011` | gold 세트 | **이 문서** |
| ② 2홉 추론 | `Q06` | owner 픽스처 | **보류 (2026-08-20, 아래 "② Q06 — 보류" 사유)** |
| ③ 의료 감별 | `Q13` **(2026-08-20 Q15에서 교체)** | owner 픽스처 | `owner_fixtures.jsonl`의 `demo_scenario` |
| ④ 처방 거절 | `Q17` | owner 픽스처 | `owner_fixtures.jsonl`의 `demo_scenario` |

**③ 교체 사유 (2026-08-20)**: 원래 ③이었던 `Q15`는 벡터·그래프 양쪽 경로가 모두
실패하는 질문이라 "그래프가 의료 감별을 구제한다"는 시나리오③의 취지를 보여주지
못합니다. `Q13`으로 바꿨습니다 — 그래프가 유일하게 문서 경계를 넘어 구제한 실측
사례입니다. 근거와 Q15 보존 사유는 아래 "③ Q13"·"③ 폐기 후보 — Q15" 절.

②③④는 픽스처 행의 `demo_scenario` 필드에 들어 있습니다. ①은 gold 세트
(`data/eval/queries/youtube_retrieval_queries.jsonl`) 질문인데 그 파일은 수정하지
않기로 되어 있어 필드에 쓸 수 없고, 그래서 매핑이 이 문서에만 있습니다.
**시나리오 ①을 바꾸려면 이 표를 고치세요.** 픽스처를 뒤져도 나오지 않습니다.

원래 ① 후보였던 `Q01`(켄넬 적응 절차)은 커버리지 판정에서 missing이 나와
`demo_scenario`가 null로 내려갔습니다. 코퍼스에 켄넬 절차가 없어서 시나리오 ①이
성립하지 않았고, 그래서 gold 세트로 옮겼습니다.

## ① q007

> 외출했다 오면 현관에 소변을 보고 장난감을 거실에 끄집어내 놨어요. 왜 그러는 건가요?

| | |
|---|---|
| query_type | `symptom_to_solution` |
| 영상 | `9FWfbXwXbP0` |
| 정답 청크 순위 | **1** (RR 1.0) — 조달 전후 동일 |
| score_gap | 0.0612 → 0.0769 → **0.0774**[^gap-0820] |
| 1-2위 차 | 0.0450 (재측정 후에도 동일 — 순위 2위까지는 여전히 영상 청크뿐) |
| top1 점수 | 0.8938 (조달로도, 이후 문서가 더 들어온 뒤에도 변하지 않음, Δtop1 +0.0000) |
| top-5 중 신규 문서 | 2건 (4·5위, 재측정 후에도 동일) |
| gold span | `q007-s1` 292280–322550ms — 현관 배변과 장난감 이동을 분리불안 신호로 설명하는 구간 |

고른 이유는 두 가지입니다. 12문항에서 gap이 가장 크고 1-2위 차도 0.045라 1위가
경쟁 청크와 확실히 벌어져 있어, 데모 중 순위가 뒤집힐 여지가 작습니다. 그리고
증상을 서술하고 이유를 묻는 형태라 owner 픽스처와 결이 같습니다 — 잘 되는 경우와
안 되는 경우를 나란히 보여줄 때 질문 형태가 달라서 생기는 착시가 없습니다.

**주의**: 이 질문의 근거 구간은 분리불안 **신호**를 설명하는 대목입니다. 시나리오 ②가
필요로 하는 켄넬 훈련의 단계·금기와는 다른 내용입니다. 단계·금기 서술 자체는
2026-08-20에 코퍼스에 들어왔지만(`docs/acquisition_list.md` 3-3), 금기 서술이
그래프 관계로 추출되지 않아 시나리오 ②는 보류입니다 (아래 "② Q06 — 보류" 참고).
①이 된다고 ②가 되는 것이 아닙니다.

## ① 대기 q011

> 혐오 자극은 효과가 있다는데 왜 쓰지 말라고 하나요?

| | |
|---|---|
| query_type | `concept` |
| 영상 | `KhNJl8kj7ks` |
| 정답 청크 순위 | **1** (RR 1.0) — 조달 전후 동일 |
| score_gap | 0.0583 → 0.0823 → **0.0856**[^gap-0820] |
| 1-2위 차 | **0.0477** (재측정 후에도 동일) |
| top1 점수 | 0.8662 (Δtop1 +0.0000) |
| top-5 중 신규 문서 | **0건** (재측정 후에도 동일) |
| gold span | `q011-s1` 36079–104190ms — 니킥을 혐오 자극으로 규정하고 효과는 있지만 감정을 줄 필요가 없다고 설명. 두 chunk에 걸침 |

1-2위 차는 q007보다 넓습니다. q007이 흔들리면 이쪽으로 바꿉니다. 다만 개념 질문이라
견주가 실제로 던지는 말투와는 거리가 있습니다.

[^gap-0820]: **2026-08-20 재측정.** 문서 코퍼스가 51청크에서 57청크로 더 늘어난 뒤
(영상 26 + 문서 57 = 83청크, fingerprint `sha256:91aa8db88f95afa2…`) `score_gap`이
다시 움직였습니다 — `⓪ gap 상승의 분해` 절의 논리 그대로, top1 점수도 1-2위 차도
그대로인데 코퍼스 평균만 더 내려간 산술 효과입니다(top1/1-2위 차 불변은 위 표에
같이 기록). q007·q011 둘 다 순위·top1은 흔들리지 않았지만, gold 12문항 전체로 보면
**이번에는 산술 효과만은 아닙니다** — 아래 "측정 출처와 유효 기간" 절 참고.

## ② Q06 — 보류 (2026-08-20)

살구뉴스 켄넬 STEP 글(`salgoonews-kennel-steps-12333`, 슬롯 3)에 요청하신 금기
서술("이동장을 잠그거나 홀로 두면 안 된다")이 원문 그대로 있지만, Stage 2 추출이
이를 관계로 잡지 못했습니다.

해당 문장이 속한 청크 8(`docchunk-7f889386`)의 추출 레코드에서 `relations`가
빈 배열이고, "잠그거나 홀로 두면 안" 구절은 그 청크의 어떤 entity의 `evidence`
필드에도 등장하지 않습니다 (entities 5건 중 가장 가까운 것은 "어린 강아지"
연령대 노드로, evidence가 "어린 강아지가 처음으로 이동장에 들어갔을 때"까지만
자르고 멈춰 있음). 금기 행위 자체가 entity로 추출되지 않아 `금기` 관계가 생성될
구조가 애초에 없었던 것으로 보이며, 그래프 전체의 `금기` 엣지가 1개뿐인 것도
같은 원인일 가능성이 있습니다.

프롬프트 v3 작성 + 코퍼스 전량 재추출이 다음 단계로 보이지만, Stage 2가 청크
증분이 아니라 전체를 매번 재추출하는 구조라(위 재적재 기록 참고) 데모 기간 내에
돌리면 다른 관계·경로 수치까지 함께 흔들리는 재추출 변동 리스크가 있습니다.
그래서 이번 데모에서는 시나리오 ②를 **보류**로 두고, 재추출은 데모 이후로
미룹니다. 상세 추출 레코드는 `data/graph/extractions_stage2.jsonl`의
`salgoonews-kennel-steps-12333` 10건, 청크 원문 대조는
`data/processed/documents/chunks/salgoonews-kennel-steps-12333.jsonl`.

## ③ Q13 (2026-08-20 교체)

> 12살 노령견인데 밤마다 잠을 안 자고 서성거리면서 벽을 보고 헛짖음을 시작했어요.
> 분리불안 훈련이 효과가 있을까요?

| | |
|---|---|
| hop_type | `multi` |
| router_target | `GRAPH` |
| 벡터 top-5 | 5건 전부 같은 문서(`강아지 분리불안 훈련,5가지 증상 및 대처법`)의 서로 다른 청크 |
| 그래프 추가 청크 | **13건** — 매칭 시드: 노령견(연령대)·분리불안(문제행동/질환/훈련법)·불안(증상)·짖음(문제행동/증상) |
| 핵심 추가 문서 | `밤에 짖는 강아지 강아지 밤새 짖는 이유는 이것!` (`docchunk-c2e164c2...`) — "노령인 경우는 관절 통증이나 치매일 가능성", "멍하니 벽을 보거나 모서리로 가는 행동... 치매의 대표적인 증상" |
| 측정 근거 | [`reports/retrieval_gap_hybrid_vs_vector_0820.md`](../reports/retrieval_gap_hybrid_vs_vector_0820.md) "Q13" 절 (evidence_chunk_ids 18건 전문) |

고른 이유: 벡터 top-5가 전부 같은 "분리불안 훈련" 문서 안에서만 순위를 매겨 **문서
경계를 넘지 못합니다** — 질문이 실제로 필요로 하는 "노령견 야간 증상 = 치매/관절통증
감별" 정보는 이 문서에 없습니다. 그래프는 "노령견"·"짖음"·"분리불안" 등의 엔티티에서
2홉을 걸어 전혀 다른 문서("밤에 짖는 강아지")를 찾아 붙였고, 그 문서가 정확히 질문이
필요로 하는 감별 정보(치매 증상 목록, 수의사 상담 권유)를 담고 있습니다. Q12·Q14·Q15와
함께 실측된 4건의 retrieval-gap 중 그래프가 **문서 경계를 넘는 연결**을 보여주는
유일한 사례입니다 (Q12는 같은 코퍼스 안 보강, Q14·Q15는 시드 자체가 안 잡힘).

**주의 — `owner_fixtures.jsonl`의 Q13 행 자체는 손대지 않았습니다.** `coverage:
"missing"`, `expected_outcome: "REFUSE"`, `refuse_reason: "GAP"`은 그래프가
생기기 **이전**, 벡터 단독 기준으로 등록된 값입니다(`note` 필드에 그렇게 적혀
있음). 이번에 바꾼 건 `demo_scenario` 필드 하나뿐입니다 — 나머지 필드가 그래프
효과를 아직 반영하지 못한 상태라는 것을 발표에서 착각하지 않도록 남깁니다.

**의료 축과의 상호작용**: 질문에 "짖음"·"분리불안"이 문자열로 있어
`classify_input_v2`가 훈련 화이트리스트로 PASS시킵니다(`docs/agenda_0825.md`
안건9의 "known_tradeoff"가 실제로 발현되는 사례) — 즉 이 질문은 입력 단계에서
의료로 걸리지 않고 그대로 생성까지 갑니다. 최종 방어선은 그래프가 끌어온 문서
자체에 담긴 "수의사 상담을 권합니다"라는 서술과, STRONG 가드(`guard_level`)가
맡습니다.

## ③ 폐기 후보 — Q15 (한계 사례로 보존, 2026-08-20)

`Q15`는 시나리오③에서 빠졌지만 **픽스처에서 지우지 않았습니다** —
`data/eval/queries/owner_fixtures.jsonl`에 그대로 남아 있고, `demo_scenario`만
`null`로 되돌렸습니다. 발표의 "한계" 파트에서 쓸 수 있는 **검색·그래프 양쪽 실패
사례**이기 때문입니다.

> 산책 중에 갑자기 털을 털고 자리에 주저앉아 안 움직이는데, 억지로 끌고 가는
> 산책 훈련을 계속해도 되나요?

[`reports/retrieval_gap_hybrid_vs_vector_0820.md`](../reports/retrieval_gap_hybrid_vs_vector_0820.md)
요약표 기준: 벡터 top-5 5건, 그래프 추가 **0건**, 매칭된 엔티티 시드 **없음**.

- **벡터 실패**: 겨냥 문서가 top-5 중 5위에 걸치긴 하지만(완전히 밖은 아님),
  생성 컨텍스트로 실제 쓰이는 top-3 안에는 못 들어옵니다.
- **그래프 실패**: 질문 문장에 그래프 노드 이름이 리터럴로 등장하지 않아
  `match_seeds`가 빈 리스트를 반환하고, `graph_search`가 아예 돌지 않습니다
  (시드 0건 → 그래프 청크 0건). `docs/graph_hybrid_retrieval_design.md` 4번
  결정(시드 매칭에 임베딩 유사도를 안 쓰는 이유)과 직접 연결되는 사례입니다 —
  "산책 중 주저앉음"이라는 표현이 그래프 노드 이름("문제행동", "증상" 등 정규화된
  엔티티명)과 문자열로 겹치지 않습니다.

Q13과 나란히 보여주면 "그래프가 항상 구제하지는 않는다 — 시드가 안 잡히면
아무것도 못 한다"는 정직한 한계를 보여줄 수 있습니다.

## 측정 출처와 유효 기간

**2026-08-20 재측정 기준**(가장 최신): `data/eval/results/combined_v4_e5_metrics.json`
(영상 26 + 문서 57 = 83청크, fingerprint `sha256:91aa8db88f95afa2…`,
`intfloat/multilingual-e5-base`, top-5). 러너는
`scripts/run_combined_retrieval_eval.py`이고, `--no-documents`로 돌리면 조달 전
영상 전용 baseline을 rank·gap·fingerprint까지 그대로 재현합니다.

조달 직후(문서 51건, 77청크) 측정치는 영상 26 + 문서 51 = 77청크,
fingerprint `sha256:86ec02a300c3a01f7cc…`였습니다. 그 사이 문서가 6건 더 늘어
지금은 57건(83청크)입니다 — 아래 세 단계 비교가 그 변화의 실측입니다:

| | gold Hit@1 | gold Hit@5 | gold MRR@5 | q003 순위 |
|---|---|---|---|---|
| 조달 전 (영상 26청크, baseline) | 0.6667 | 0.8333 | 0.7361 | 3위 |
| 조달 직후 (문서 51건, 77청크) | 0.6667 | 0.8333 | 0.7292 | 4위 |
| **2026-08-20 (문서 57건, 83청크)** | 0.6667 | **0.75** | **0.7083** | **미검출(top-5 밖)** |

**Δtop1은 gold 12문항 전부 여전히 0.0000입니다** — 문서가 어느 gold 질문에서도 1위를
밀어내지 못한 것은 조달 직후와 동일합니다. 하지만 이번에는 순위가 바뀐 것이
q003뿐이라는 말을 더 이상 할 수 없습니다: q003은 4위에서 아예 top-5 밖으로
떨어졌고, 그 결과 Hit@5·MRR@5가 조달 직후보다 더 내려갔습니다 — "코퍼스가
커지며 평균만 내려간 산술 효과"였던 조달 직후 단계와 달리, 이번 단계는 **q003
한 문항의 실제 검색 실패**가 섞여 있다는 뜻입니다. q003 자체는 이 네 개 데모
시나리오에 들어있지 않아 이 문서에서 더 파고들지는 않지만, `docs/acquisition_list.md`
조달 후 체크리스트를 돌릴 때 함께 기록해 두어야 합니다.

**시나리오① 유지 확정.** q007은 정답 청크가 1위를 지켰고 top1 점수도 그대로입니다.
4·5위에 분리불안 문서가 들어왔지만 근거 청크를 밀어내지 못했고, 문서가 6건 더
늘어난 2026-08-20 재측정에서도 이 결과는 그대로입니다. q011은 top-5에 문서가
한 건도 들어오지 않아 더 깨끗하지만, 개념 질문이라 견주 말투와는 거리가 있다는
판단은 그대로여서 대기로 둡니다.

## dry-run 답변에 대하여

리포트 [`reports/combined_corpus_coverage.md`](../reports/combined_corpus_coverage.md)
② 절의 q007·q011 답변은 **Claude가 `grounded-answer-ko-v1` 규칙을 따라 수기로 작성한
dry-run**입니다. 모델 API를 호출해 받은 생성물이 아닙니다. 원본은
`data/eval/generation/answers_dryrun_scenario.jsonl`이며 각 레코드의 `produced_by`에
같은 내용이 적혀 있습니다.

**금요일 동결 런에서 실제 생성으로 교체합니다.** 그때까지 이 답변은 "검색된 청크로
답이 되는지"를 사람이 보기 위한 대용물이지, 생성 품질의 근거가 아닙니다.

## 다시 무효가 되는 조건

`score_gap`은 top1에서 코퍼스 평균을 뺀 값이라 청크가 늘거나 줄면 12문항 전체의 gap이
함께 움직입니다. 이번 조달에서 실측된 이동폭이 그 증거이고, 위 "측정 출처와 유효
기간" 절의 2026-08-20 재측정이 실제로 다시 무효가 된 사례입니다(문서 51→57건).
코퍼스를 또 건드리면 `docs/acquisition_list.md`의 조달 후 체크리스트를 돌리고 이
표를 다시 채우세요.
