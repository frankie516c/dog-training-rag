# TODO — 안전 게이트 · 검색 품질 작업

작업 단위 체크리스트. 상태는 `[ ]` 대기 / `[~]` 진행 중 / `[x]` 완료 / `[-]` 보류.
완료할 때마다 `STATUS.md`의 "지금 상태"를, 근거·수치가 나오면 `HISTORY.md`를 갱신한다.

세션이 끊겨도 여기부터 다시 읽으면 이어갈 수 있게 쓴다.

---

## A. 화이트리스트 우선순위 확정 (선행) — **완료 2026-08-28**

결론: 경계 → 화이트리스트. 위해 구멍 6/6 → 0/6, 통과 대조군 4/4 유지, 동결 계약 위반 0.

- [x] A-1. `gate()`에서 경계 검사를 화이트리스트 위로 되돌린다
- [x] A-2. `test_whitelist_precedence_lets_harm_through`를 "막힌다" 계약으로 뒤집는다
- [x] A-3. 통과 대조군 4건 · 동결 25건 계약이 그대로인지 재측정
- [x] A-4. `gate()` docstring의 우선순위 설명을 결과에 맞춘다

## 1. 커밋 · PR — **완료 2026-08-28**

커밋 `ddce653` / DAENGS PR #45 (`fix/training-rag-safety-decision` -> `dev`).

- [x] 1-1. `git add -f data/eval/queries/gate_pass_controls_v1.jsonl` (gitignore 예외)
- [x] 1-2. dog-training-rag-retrieval 커밋
- [x] 1-3. DAENGS_dev `fix/training-rag-safety-decision` 커밋 · push · PR
- [~] 1-4. DAENGS backend venv의 `ml` 그룹 상태를 사용자에게 확인받는다

## 2. 게이트가 거짓말하는 것 닫기 — **완료 2026-08-28 (판정: 신호 없음)**

`reports/retrieval_gate_signal_0828.md`. top_score·margin·top1-top2·문서 다양성·점수 편차
**다섯 개 전부 hit/miss 가 겹쳐** 임계값으로 못 가른다. 규칙은 안 바꿨고, 0.70 이 죽은
분기라는 사실만 상수 이름·문서·테스트로 고정했다. 동결 25건 일치율 88.0% 그대로.

죽은 임계값(`top_score < 0.70`)과 계산만 하고 안 쓰는 `margin_topk`.

- [x] 2-1. 동결 25건에서 hit/miss별 `top_score`·`margin` 분포를 뽑는다
      (`data/scratch/retrieval_reranking_0827_v5/results.json` — DB 불필요)
- [x] 2-2. 분리 가능한 신호가 있는지 판정한다. **없으면 없다고 기록하고 2-3을 건너뛴다**
- [x] 2-3. 판정 규칙을 고치고 회귀 테스트를 추가한다
- [x] 2-4. 동결 25건 decision 일치율이 88.0%에서 어떻게 변하는지 기록

## 3. cross-encoder 리랭커 — **완료 2026-08-28 (판정: 기각)**

`reports/cross_encoder_rerank_0828.md`. `NO_CROSS_ENCODER_ADOPTION` — 모든 품질 지표가
나빠졌다 (Hit@1 21.1→10.5%, Hit@4 47.4→42.1%, MRR@4 .307→.224). 코드 무변경.
깊은 것은 끌어올리지만(평균 순위 14.16→9.95) top-4 안의 순서를 망친다. 게이트 신호도
중앙값은 갈리나(0.851 vs 0.338) 범위가 겹쳐 19건으로는 판단 불가. 지연 64.4초/질문.

- [x] 3-1. ~~Docker · pgvector 기동~~ → **불필요.** 서빙 청크 83개가 디스크에 있고
      로컬 인코딩으로 dense 기준선이 소수점까지 재현된다 (하네스가 시작할 때 검사)
- [x] 3-2. `scripts/compare_cross_encoder_rerank.py` 작성 (기존 스크립트는 DB 의존이라 새로)
- [x] 3-2b. 실행 — OOM 으로 두 번 중단. 인코더 해제 + 배치 4 로 완주 (20.4분)
- [x] 3-3. 동결 25건으로 Hit@1 / Hit@4 / MRR 재측정, dense 기준선(47.4%)과 비교
- [x] 3-3b. **리랭커 점수가 hit/miss 를 가르는지도 같이 측정** (2번이 넘긴 숙제 —
      가르면 게이트를 그 점수 위에 세우고, 못 가르면 게이트는 관련성 판단을 포기한다)
- [x] 3-4. 기존 dense 정답 9건이 top-4 밖으로 밀리지 않는지 확인 (0827 실험의 탈락 기준)
- [-] 3-5. ~~채택 시 상한 풀기~~ → **기각이므로 해당 없음.** 다만 상한 분리 자체는
      여전히 후보다 (리랭커가 깊은 것을 5~16위로 끌어올렸으나 4위 안에 못 넣었다)
- [x] 3-6. 보고서 작성 (`reports/`)

## 4. 다음 — 순위 개선 계열이 소진됐다

0827(BM25·RRF·lexical)과 0828(cross-encoder)로 **후보 재정렬 방식 네 가지가 전부 기각**됐다.
같은 65청크 위에서 순서를 바꾸는 것으로는 Hit@4 가 안 오른다. 남은 가설은 상류다.

- [ ] 4-1. `top_k: le=4` 상한 분리 — 검색 10 → 프롬프트 4. 상한은 검색이 아니라 Gemma 4B
      취약성 때문에 걸린 것이다. **재정렬은 dense 에 맡긴다** (리랭커는 상위 순서를 망친다)
- [ ] 4-2. **인제스트 키 버그 먼저** (아래) — 4-3 의 선행 조건
- [ ] 4-3. 청킹 — 앵커가 9~47위에 흩어져 있다. 정답 조각이 질문과 어휘적으로 먼지 본다
- [ ] 4-4. gold 계약 — oq0032 한 행이 지표를 5%p 움직였다. 19건이라는 크기 자체가 제약이다

## 보류 (이번 범위 아님, 잊지 않기 위해 적어 둠)

- [-] 인제스트 키 버그 — `ON CONFLICT(chunk_id)`가 `embedding_model`을 덮어써
      같은 청킹의 두 임베딩 런이 공존할 수 없다. **청킹·임베딩을 건드리기 직전에 반드시 먼저.**
      리랭커는 재적재가 없어 이 버그를 안 건드린다.
- [-] 운영 · 보안 — 훈련 RAG가 PM2 · compose · deploy 어디에도 없음 / 상하류 타임아웃
      45초 vs 180초 어긋남 / `0.0.0.0:8010` 무인증 LAN 노출. **시연 일정이 잡히면 최우선.**
- [-] 콘솔 `검색 점검` 화면의 훈련 갈래가 근거 본문·점수·인용 검증을 안 보여 줌
- [-] 질문 · 판정 되먹임 경로 없음 (평가셋이 실제 질문 위에서 자라지 못함)
