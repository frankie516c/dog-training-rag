# STATUS — 안전 게이트 · 검색 품질

마지막 갱신: 2026-08-28

## 지금 상태

**A(안전 게이트 오차단) 끝. 1(커밋·PR) 진행 중. 2·3은 대기.**

- `scripts/pgvector_runtime.py`의 안전 경계가 **한 음절 정규식에서 2글자 이상 구절 사전으로** 바뀌었다.
  `약`·`물었`이 정상 훈련 질문을 막던 문제가 닫혔고, 동결 평가셋 25건의 판정은 한 건도 바뀌지 않았다.
- 판정 순서는 **경계 → 의료(화이트리스트 우선) → 검색**이다. 화이트리스트는 의료 사전만 이기고
  물리적 위해 경계는 못 이긴다 — 그 반대로도 만들어 재 봤고, 근거는 `HISTORY.md` 4절에 있다.
- 안전 거절이 더 이상 "자료에 없다"고 말하지 않는다. DAENGS 쪽도 `SAFETY_REFUSAL`로 따로 표시한다.
- 테스트 **382 passed / 8 skipped** (작업 전 371). 추가 11건.

## 다음에 이 폴더를 열면

`TODO.md`의 첫 번째 `[ ]`부터 이어서 하면 된다. 지금은 **1-2(커밋)**이다.

## 아직 안 고친 것 중 중요한 둘

- **검색 품질이 이 작업으로 좋아지지 않았다.** Answerable anchor Hit@4는 여전히 47.4%다.
  안전 게이트를 고친 것이지 검색을 고친 게 아니다. 그게 TODO의 2·3이다.
- **인제스트 키 버그가 살아 있다.** 같은 청킹을 다른 임베딩 모델로 재적재하면 기존 런이
  조용히 사라진다. 리랭커(3번)는 재적재가 없어 안전하지만, **청킹·임베딩을 건드리기 전에는
  반드시 먼저 고쳐야 한다.**

## 손대면 안 되는 것

- `data/eval/queries/training_api_eval_v1.jsonl` — 동결 평가셋. 행을 추가하면 88.0% /
  47.4% 기준선과의 비교가 깨진다. 새 대조군은 `gate_pass_controls_v1.jsonl`에 넣는다.
- `data/guardrail/*.json` — 사람이 확인한 사전이다. 항목을 늘릴 때는 `risk_notes`에
  왜 그 구절인지 같이 적는다. 한 음절 항목은 금지이고, 그 규칙은 테스트가 강제한다.

## 환경 메모

- 이 저장소에 `pytest`가 의존성으로 없다. `uv run --with pytest python -m pytest -q`로 돌린다.
- `data/eval/queries/*`가 gitignore다. 대조군 파일은 `git add -f`로 넣었다.
- 2번은 저장된 실험 결과만으로 되지만, 3번은 Docker · pgvector 기동이 필요하다.
