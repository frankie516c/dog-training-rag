# 라이선스 전제 점검 — 공개 저장소와 `docs/SOURCES.md`의 어긋남 (2026-08-25)

`docs/SOURCES.md`가 "이 저장소는 개인 학습·포트폴리오용 **비공개**를 전제로
합니다"로 시작하는데 저장소는 public입니다. 그 어긋남이 실제로 무엇을 뜻하는지
확인만 했습니다.

**아무것도 고치거나 지우지 않았습니다.** 확인과 보고까지입니다.

---

## 1. 크롤 원자재는 커밋된 적 없다 — 확인됨

과거에 한 번이라도 커밋됐는지를 **전체 ref 기준**으로 봤습니다
(`git log --all --diff-filter=A`, 현재 gitignore 여부와 무관).

| 확인 항목 | 결과 |
|---|---|
| `data/raw/` 아래 추가 이력 | **0건** |
| `data/` 아래 추가 이력 전체 | 30경로 (eval·guardrail·reviews·profiles) |
| `data/` 아래 삭제 이력 (`--diff-filter=D`) | **0건** — "넣었다 뺀" 흔적 없음 |
| `data/processed/` | `evidence_cards.jsonl` 1건뿐 |

`data/processed/evidence_cards.jsonl`은 HEAD에 없고
`origin/archive/data/evidence-seed-v0` 등 아카이브 브랜치에만 있습니다.
내용은 PMC 오픈액세스 논문을 **한국어로 요약한 주장 8건**(`claim` 최대 110자)
이고 원문 인용이 아닙니다. 소스는 URL·DOI·PMCID로 참조만 합니다.

### 크롤 원자재 본체

`../scrapper/`(크롤러와 `data/blog_raw*` 원자재가 있는 곳)를 따로 확인했습니다.

- **remote 없음** — 로컬 전용 저장소입니다. 어디에도 push된 적이 없습니다.
- 추적 파일 6개(`.gitignore`, 스크립트 3개, `data/subs_crossval/`의 README·SOURCES).
- `posts.jsonl` 등 수집물 커밋 이력 **0건**.

**여기까지는 규칙이 지켜졌습니다.**

---

## 2. 그러나 원문은 다른 경로로 커밋됐다

파일은 안 올라갔는데 **텍스트는 평가 산출물을 경유해 올라갔습니다.**

원자재(크롤 1,098건 + 현행 코퍼스 청크)에서 공백 제거 후 40자 shingle
집합(170만 개)을 만들고, HEAD에 추적되는 모든 텍스트 파일을 대조했습니다.
아래는 전부 **`origin/main`에 존재**합니다 — 즉 이미 공개돼 있습니다.

| 파일 | 일치 구간 | 최장 연속 | 원문 일치 합계 |
|---|---:|---:|---:|
| `data/eval/results/combined_v4_e5_metrics.json` | 413 | **376자** | **83,278자** |
| `data/eval/queries/_synthetic_prompt.md` | 20 | 335자 | 4,943자 |
| `reports/owner_fixtures_coverage.md` | 63 | 115자 | 6,819자 |
| `reports/combined_corpus_coverage.md` | 40 | 124자 | 4,524자 |
| `frozen/frozen_stage2_0820.jsonl` | 83 | 104자 | 4,819자 |
| `docs/acquisition_list.md` | 3 | 216자 | 428자 |
| `scripts/ingest_pdf_documents.py` | 3 | 69자 | 159자 |

뒤 두 건은 오탐입니다 — `acquisition_list.md`의 216자는 퍼센트 인코딩된 URL,
`ingest_pdf_documents.py`의 69자는 AVSAB 논문 제목·학회명 메타데이터입니다.

### 2-1. `combined_v4_e5_metrics.json` — 가장 큰 건 (410KB)

검색 평가 산출물인데 `owner_fixtures[].top_k[].text` / `gold[].top_k[].text`
필드에 **검색된 청크의 본문 전체**가 들어갑니다. 문서 청크와 유튜브 자막 청크
양쪽 다입니다.

- 텍스트 필드 307개, 총 116,681자. 그중 302개가 100자 이상.
- `.gitignore`는 `data/eval/results/*`를 제외하면서
  **`!data/eval/results/*_metrics.json`으로 이 확장자를 명시적으로 화이트리스트**
  합니다. 그 예외가 원문이 들어온 통로입니다.
- `.gitignore` 주석은 이 예외의 근거를 "이름 붙인 실험 스냅샷은 추적한다 …
  코퍼스를 다시 만들면 되살릴 수 없다"로 적고 있습니다. 스냅샷을 남긴다는
  의도는 타당하지만, **스냅샷이 청크 본문까지 통째로 들고 있다는 점은
  그 주석이 고려하지 않았습니다.**

### 2-2. `_synthetic_prompt.md` — 유튜브 자막 원문 (17KB)

합성 질문 생성에 쓴 LLM 프롬프트인데, 지문으로 **유튜브 자막을 그대로**
붙여 넣었습니다. 지문 10여 개, 각 400~500자, `[음악]`·`>>` 화자 표시가
그대로 남아 있습니다.

`docs/SOURCES.md`는 같은 문서 안에서 **"YouTube ToS도 자막·영상 스크래핑을
명시적으로 금지합니다"**라고 적고 있습니다. 이 파일이 그 문장과 가장 직접
부딪히는 지점입니다.

### 2-3. 리포트 2건 — 사람이 인용한 게 아니라 하네스가 넣은 것

`reports/owner_fixtures_coverage.md`와 `reports/combined_corpus_coverage.md`는
둘 다 머리에 **"이 파일은 `scripts/...`이 생성합니다. 직접 편집하지 마세요"**가
붙어 있습니다. 인용 블록은 최장 189자·208자이고 **300자 이상은 0개**입니다.
사람이 장문을 옮긴 것이 아니라, 평가 하네스가 top-k 근거를 리포트에 찍는
설계 때문에 쌓인 것입니다.

### 2-4. 이번 세션에 제가 커밋한 리포트

`reports/document_parsing_validation_0825.md`,
`reports/p1_boilerplate_candidates_0825.md` 둘 다 **40자 이상 연속 일치 0건**
입니다. 사례를 최소 길이로만 인용한 것이 대조로 확인됩니다.

### 2-5. 이 측정의 한계 — 실제 양은 이보다 많을 수 있다

shingle 집합을 **현재 남아 있는** 원자재와 **현행** 코퍼스로 만들었습니다.
따라서 **이미 사라진 옛 청크의 원문은 탐지되지 않습니다.**

- `combined_v4_e5_metrics.json`의 텍스트 필드 총량은 116,681자인데 원자재와
  일치한 것은 83,278자입니다. 차이 33,403자는 지금 코퍼스에 없는 청크로
  보입니다 — 탐지 안 됐을 뿐 원문인 것은 마찬가지입니다.
- `baseline_v1_e5_metrics.json` 등 v1·v2 스냅샷은 매칭 0으로 나왔지만,
  이는 v1 청크가 디스크에 없어서일 가능성이 큽니다. **"원문이 없다"는
  결론으로 읽으면 안 됩니다.**

정확한 총량을 알려면 각 스냅샷의 텍스트 필드를 직접 세야 합니다. 이번엔
`combined_v4`만 그렇게 셌습니다.

---

## 3. `docs/SOURCES.md`의 전제와 현재 상태가 어긋나는 지점 — 목록

요청대로 목록만 적습니다. 판단이나 조치는 넣지 않았습니다.

| # | 문서의 전제·규칙 | 현재 상태 |
|---|---|---|
| 1 | "이 저장소는 **개인 학습·포트폴리오용 비공개**를 전제로 합니다. 그 전제가 바뀌면 소스 구성을 다시 짜야 합니다." | 저장소가 public. 문서가 스스로 정한 재검토 조건이 이미 발동한 상태. |
| 2 | 공개 전환 시 할 일 ①2층(공공·기관) 비중 올리기 | 미실행. 2층은 AVSAB PDF 3건(115청크). |
| 3 | 〃 ②여러 훈련사 소스를 섞어 특정 개인 복제 방지 | 부분 실행. 인제스트 문서 14건의 저자는 6곳으로 늘었으나, **크롤 원자재 829건 중 582건(70%)이 단일 필자**(yoonsu3454). |
| 4 | 〃 ③원문 그대로의 장문 재현을 차단하는 조치 | 미실행. 2절이 그 결과. |
| 5 | 〃 ④수집물은 어떤 경우에도 저장소에 커밋하지 말 것 | **파일 단위로는 지켜졌고, 텍스트 단위로는 지켜지지 않았습니다.** `.gitignore`의 `*_metrics.json` 화이트리스트가 통로. |
| 6 | "**한 사람의 채널만 통째로 떠서** 그 말투와 처방을 그대로 재현하는 모델이 가장 불리한 자리에 섭니다." | 인제스트는 2건뿐이지만 **원자재 구성이 정확히 그 모양**. 829건 인제스트 안을 검토 중이었다는 점에서 특히. |
| 7 | "YouTube ToS도 자막·영상 스크래핑을 명시적으로 금지합니다." | 자막 원문이 `_synthetic_prompt.md`·`owner_fixtures_coverage.md`로 `origin/main`에 공개돼 있음. |
| 8 | 공정이용 불리 케이스 "원저작물의 문장·구성·표현을 **실질적으로 동일·유사하게 재현**" | 최소 83,278자가 공개 저장소에 그대로 존재. |
| 9 | "조사 기준일 2026-08-11. 링크가 죽었으면 이 문서를 먼저 갱신할 것." | 2주 경과. 그 사이 추가된 소스(easiestip·핏펫·wayopet)는 어제 제가 추가한 "수집된 블로그 풀 현황" 절 외에는 반영 없음. |
| 10 | 인제스트 문서 front matter의 `license: 출처 명기 · 데모 내부 사용 · **재배포 없음**` | public 저장소 게시와 "재배포 없음"의 관계가 어디에도 정의돼 있지 않음. 해당 `.md` 파일들은 미커밋이므로 현재 직접 충돌은 아님. |

### 목록에 넣지 않은 것 (확인했으나 문제 없음)

- `/premium/`·`/member/`·`/paid/` 차단은 `check_url_allowed()`로 코드 강제되고
  있고, 829건 전부 통과합니다(위반 URL 0건).
- `data/eval/results/owner_fixtures_topk.jsonl`(35,011자, 원문 100건)은
  **gitignore로 정확히 제외**돼 있습니다. 추적되지 않습니다.
- `../scrapper` 저장소에는 remote가 없고 수집물 커밋 이력이 없습니다.

---

## 4. 판단하지 않은 것

- **어떤 조치가 필요한지 정하지 않았습니다.** 이력 재작성(force push)은
  아카이브 브랜치 9개를 포함해 원격 이력 전체를 건드리는 일이고, 되돌릴 수
  없습니다. 승인 없이 손댈 종류의 작업이 아닙니다.
- 저장소를 private으로 되돌리는 것과 커밋된 원문을 제거하는 것 중 무엇이
  맞는지도 판단하지 않았습니다. 전자는 한 번의 설정 변경이고 후자는 이력
  재작성입니다. 선택지의 성격이 다릅니다.
- 공정이용 성립 여부는 법적 판단이라 하지 않았습니다. 위 표는 문서가 스스로
  적어둔 기준과 현재 상태의 대조일 뿐입니다.

---

# 2차 점검 (2026-08-25 오후)

1차는 HEAD만 봤습니다. 이번엔 **모든 ref의 모든 blob**을 봤고, 조치 가능 범위를
정리했습니다.

## 5. 원문 노출 파일 전체 목록 — 이력 전체 기준

`git rev-list --all --objects`로 나온 오브젝트 전부를 대조했습니다.
텍스트 blob **512개 검사 → 33개 일치 → 고유 경로 15개**.
원자재 shingle 집합은 크롤 6풀 + 수동 3건 + 현행 코퍼스 + 유튜브 자막·트랜스크립트
(문서 1,761건)로 만들었습니다.

| 경로 | blob 버전 | 최장 연속 | 최대 노출량 | 성격 |
|---|---:|---:|---:|---|
| `data/eval/results/combined_v4_e5_metrics.json` | **8** | 376자 | **83,278자** | 청크 본문 통째 |
| `reports/owner_fixtures_coverage.md` | 2 | 115자 | 6,819자 | 하네스 생성 스니펫 |
| `data/eval/queries/_synthetic_prompt.md` | 1 | 335자 | 4,943자 | **유튜브 자막 원문** |
| `frozen/frozen_stage2_0820.jsonl` | 1 | 104자 | 4,819자 | 추출 원문 |
| `reports/combined_corpus_coverage.md` | 6 | 124자 | 4,524자 | 하네스 생성 스니펫 |
| `docs/acquisition_list.md` | 4 | 216자 | 428자 | 대부분 URL(오탐) |
| `docs/data-research/source-audits/pmc7743949-…` | 1 | 97자 | 194자 | 논문 제목(오탐) |
| `scripts/ingest_pdf_documents.py` | 2 | 69자 | 159자 | AVSAB 제목(오탐) |
| `reports/retrieval_gap_hybrid_vs_vector_0820.md` | 1 | 57자 | 155자 | 스니펫 |
| `data/sources/source_registry.jsonl` | 1 | 97자 | 152자 | 논문 제목(오탐) |
| `docs/data-research/source-audits/pmc7387681-…` | 1 | 55자 | 110자 | 논문 제목(오탐) |
| `docs/data-research/source-audits/avsab-dominance-…` | 1 | 78자 | 78자 | 논문 제목(오탐) |
| `reports/q003_top5_investigation_0820.md` | 1 | 60자 | 60자 | URL 포함(오탐) |
| `data/eval/queries/gold_batch1.jsonl` | 1 | 45자 | 45자 | 미미 |
| `scripts/write_gold_labels_batch1.py` | 1 | 45자 | 45자 | 미미 |

- 경로 기준 합계 **105,809자**. `combined_v4_e5_metrics.json` 하나가 79%입니다.
- 그 파일은 이력에 **8개 버전**이 있고 누적 543,213자입니다(같은 파일의 시점별
  스냅샷). 현재 `origin/main` tip에 있는 것은 74,077자짜리 버전이고,
  83,278자짜리 최대 버전은 로컬 브랜치 tip에서 도달합니다.
- 논문 제목이 걸린 4건은 오탐입니다 — AVSAB·PMC 논문 **제목**이 PDF 청크에도
  들어 있어 매칭된 것이고, 서지 정보는 인용이 아닙니다.
- `docs/data-research/`는 HEAD에 없고 아카이브 브랜치에만 있습니다.
- **1차에서 놓친 것은 전부 소액입니다.** 5개 → 15개로 늘었지만 새로 발견된 10개의
  합계는 1,426자이고 그중 상당수가 오탐입니다. 1차의 결론은 바뀌지 않습니다.

### 이 측정으로도 못 잡는 것

- 지금 디스크에 없는 옛 코퍼스(v1·v2 청크)에서 뽑힌 원문. `combined_v4`의 텍스트
  필드 총량은 116,681자인데 일치한 것은 83,278자입니다. 나머지 33,403자도 원문인
  것은 같고, 대조할 원본이 사라져 안 잡힌 것입니다.
- 40자 미만으로 흩어진 인용.

## 6. `combined_v4_e5_metrics.json`의 텍스트 필드 — 불필요, 스키마에서 제거함

### 확인 결과: 회귀 검증에 필요 없습니다

근거 네 가지입니다.

1. **읽는 코드가 없습니다.** `top_k[].text`를 커밋된 metrics 파일에서 되읽는
   스크립트·테스트가 하나도 없습니다. metrics를 로드하는 4개 스크립트
   (`analyse_refusal_signals`·`analyse_score_gap`·`diagnose_synthetic_misses`·
   `probe_out_of_corpus`)는 전부 **코퍼스에서** `text`를 읽지 metrics에서 읽지
   않습니다.
2. **다른 스냅샷 6개는 애초에 `text`가 없습니다.** `data/eval/results/`의
   `*_metrics.json` 7개 중 텍스트 필드를 가진 것은 `combined_v4` 하나뿐입니다.
   나머지는 `evaluate_youtube_retrieval.py`가 만들었고 `text`를 쓰지 않습니다.
   **청크 크기를 v3로 확정한 v1↔v3 비교가 바로 그 텍스트 없는 스냅샷들로
   수행됐습니다**(`chunking_config.py` 독스트링). 회귀 검증에 본문이 필요했다면
   그 비교가 불가능했어야 합니다.
3. **식별에 필요한 것은 남습니다.** `chunk_id`·`rank`·`score`·게이트 판정·
   `first_relevant_rank`·`reciprocal_rank`가 그대로입니다. `where`는 이미
   사람이 읽는 이름을 담고 있습니다(`문서 · 슬개골 탈구 단계별 증상`).
4. **`.md` 리포트는 영향 없습니다.** 리포트는 파일이 아니라 메모리상의 payload에서
   생성되므로 150자 스니펫을 그대로 출력합니다.

### 실제 비용 — 있습니다

`.gitignore` 주석이 이 예외를 둔 이유는 "코퍼스를 다시 만들면 되살릴 수 없다"
입니다. 본문을 빼면 **코퍼스가 재생성돼 `chunk_id`가 바뀐 뒤에는 옛 스냅샷의
`chunk_id`를 실제 텍스트로 되짚을 수 없습니다.** 이건 실재하는 손실입니다.

다만 그 손실은 다른 6개 스냅샷이 이미 감수하고 있는 것과 같은 종류이고,
`where`·`slot`·`chapter_title`이 남으므로 "어떤 성격의 청크가 몇 위였나"는
읽힙니다. 본문 전체를 공개 저장소에 남기는 대가로 살 만한 값은 아니라고
판단했습니다.

### 조치

`scripts/run_combined_retrieval_eval.py`에 `without_chunk_text()`를 추가하고
JSON 직렬화 직전에만 적용했습니다. 리포트 생성은 그대로입니다. 테스트 3개 추가,
전체 281개 통과.

**이건 앞으로 생성될 스냅샷에만 적용됩니다. 이미 커밋된 8개 버전은 그대로
남아 있습니다.** 7절 참고.

## 7. `_synthetic_prompt.md` — SOURCES.md와 정면으로 상충합니다

재확인 결과입니다.

- 지문 **20개, 총 6,671자**. 전부 보듬TV 영상 자막입니다
  (`_synthetic_mapping.json`이 `video_id`·`start_ms`까지 매핑해 둡니다).
- `[음악]`·`[웃음]`·`>>` 화자 표시가 그대로 남아 있는 원문 그대로입니다.
- 같은 저장소 `docs/SOURCES.md` 112행:
  **"YouTube ToS도 자막·영상 스크래핑을 명시적으로 금지합니다."**

상충 맞습니다. 다만 정확히 적어두면 — 이 문장은 "공개·서비스화로 방향이 바뀔
경우" 무엇이 불리한지를 설명하는 문단에 있고, 저장소가 public이 된 지금 그
가정이 발동한 상태입니다. 수집 자체(`--write-auto-subs`를 뺀 수동자막 수집)에
대한 판단과, **그 자막을 공개 저장소에 원문으로 게시한 것**은 별개의 문제이고
후자가 지금 걸려 있는 쪽입니다.

## 8. "고쳐서 새로 커밋"으로는 끝나지 않습니다

**이것이 이 리포트의 핵심입니다.**

6절의 스키마 수정도, 파일을 지우는 커밋도, **과거 커밋의 내용을 바꾸지
않습니다.** git 커밋은 불변이고, 지우는 커밋은 "지웠다는 사실"을 이력에 한 줄 더
추가할 뿐입니다. `combined_v4_e5_metrics.json`의 8개 버전은
`git show <sha>:data/eval/results/combined_v4_e5_metrics.json` 한 줄로 지금도
전부 꺼낼 수 있고, GitHub에서는 커밋 URL로 브라우저에서도 열립니다.

**이력에서 실제로 제거하려면 `git filter-repo` 또는 BFG로 이력을 재작성하고
force push 해야 합니다. 다른 방법이 없습니다.**

### 이력 재작성을 할 경우와 안 할 경우

| | 안 할 경우 | 할 경우 (filter-repo + force push) |
|---|---|---|
| 과거 커밋의 원문 | 그대로 접근 가능 | ref에서 도달 불가 |
| 커밋 해시 | 유지 | **전부 변경** — 17개 원격 브랜치 + 태그 `vector-baseline-v3` |
| 다른 로컬 클론 | 영향 없음 | 재클론 필요 (이 PC에 최소 2개: `-retrieval`, `-collision-dogs`) |
| 진행 중인 gold 작업 | 영향 없음 | **다른 세션의 브랜치가 깨집니다** |
| 이미 받아간 사본 | 회수 불가 | **회수 불가 (동일)** |
| GitHub 서버의 dangling 객체 | 남음 | **남을 수 있음** — 아래 |

### force push 후에도 남는 것 (일반적인 GitHub 동작, 이 저장소에서 검증한 것 아님)

- force push로 참조가 끊긴 커밋은 GitHub 저장소에 **dangling 상태로 남고, SHA를
  아는 사람은 `github.com/…/commit/<sha>`로 계속 열 수 있습니다.** GitHub는
  즉시 GC하지 않습니다. 확실히 지우려면 **GitHub Support에 GC를 요청**해야
  한다는 것이 GitHub 자신의 안내입니다.
- fork가 있으면 객체가 fork 네트워크에 남아 원본을 지워도 접근 가능합니다.
  **이 저장소는 fork 0개라 이 경로는 없습니다** (아래 실측).
- GitHub 코드 검색 인덱스는 재크롤 전까지 삭제된 내용을 들고 있을 수 있습니다.
- **Software Heritage·GHArchive 같은 제3자 아카이브의 수집 여부는 확인하지
  못했습니다.** 외부 조회가 필요합니다. 원하시면 확인하겠습니다.

### 이 저장소의 실측 노출 지표 (GitHub API, 2026-08-25)

| 항목 | 값 |
|---|---|
| 공개 여부 | **PUBLIC** (생성 2026-08-12) |
| fork | **0** |
| star / watcher | 0 / 0 |
| clone (14일) | **129회 / 고유 85** |
| view (14일) | 119회 / **고유 6** |

읽는 법에 주의가 필요합니다.

- **clone 85 고유 vs view 고유 6.** 사람이 저장소를 보고 클론하는 패턴이면 view가
  clone보다 많거나 비슷합니다. 반대로 나온 것은 **자동화 클라이언트**(CI, 봇,
  코드 스캐닝 크롤러, 또는 이 PC의 도구가 매번 새 클론을 만드는 경우)의 서명에
  가깝습니다. 이 PC에만 로컬 클론이 최소 2개 있습니다.
- **GitHub 트래픽 API는 "누가" 클론했는지 알려주지 않습니다.** 85건 중 몇 건이
  제3자인지 이 데이터로는 판별할 수 없습니다. "제3자 사본이 있다"고도
  "없다"고도 단정하면 안 됩니다.
- 일별로는 8/13~8/14에 몰려 있습니다(16 + 37회). 그 시점에 무엇을 했는지는
  이 데이터 밖입니다.

### private 전환은 무엇을 해결하는가

- **해결합니다**: 익명 접근이 전부 막힙니다. tip뿐 아니라 **dangling 커밋도
  SHA를 알아도 인증 없이는 못 엽니다.** 코드 검색 인덱스에서도 빠집니다.
  fork가 0이라 "fork가 public으로 떨어져 나가는" 문제도 없습니다.
- **해결하지 않습니다**: 이미 클론된 사본, 제3자 아카이브의 스냅샷, 캐시된
  검색 결과. 이건 이력 재작성으로도 해결되지 않는 것과 **같은 집합**입니다.
- **성격이 다릅니다**: private 전환은 설정 한 번이고 되돌릴 수 있으며 진행 중인
  다른 세션 작업에 영향이 없습니다. 이력 재작성은 되돌릴 수 없고 17개 브랜치의
  해시를 전부 바꿉니다.

정리하면 — **지금 계속되는 공개 노출을 멈추는 것은 private 전환이 하고, 이력에서
원문 자체를 없애는 것은 재작성이 합니다. 이미 나간 사본은 둘 다 못 되돌립니다.**
둘은 배타적이지 않고, 순서를 정할 수 있는 별개의 조치입니다.

**결정은 하지 않았고 아무 조치도 취하지 않았습니다.** 이력 재작성은 지시대로
진행하지 않았습니다.

## 9. 결정 전까지 지키는 것

- `.gitignore` 예외 규칙(`*_metrics.json`, `*_report.md`)에 **새로운 텍스트 필드를
  추가하지 않습니다.** 6절의 수정은 필드를 뺀 것이지 더한 것이 아닙니다.
- gold 라벨링·코퍼스 확대는 계속 진행합니다. 다만 새 산출물이 저 두 예외 경로에
  본문을 싣지 않는지 확인하고 넣습니다.

---

# 3차 — 확인 답변과 조치 (2026-08-25 저녁)

**푸시하지 않았습니다.** private 전환 완료 통보 전까지 로컬 커밋만 유지합니다.
위험 순서 정정(자막 게시 > 스키마)을 반영해 자막 건을 먼저 처리했습니다.

## 10. 확인 답변 (1~3)

### 10-1. `_synthetic_prompt.md` / `_synthetic_mapping.json` — 둘 다 HEAD·origin/main에 있었음

| 경로 | HEAD | origin/main |
|---|---|---|
| `data/eval/queries/_synthetic_prompt.md` | 있음 | 있음 |
| `data/eval/queries/_synthetic_mapping.json` | 있음 | 있음 |

참조 코드는 둘뿐입니다.

| 스크립트 | 관계 |
|---|---|
| `scripts/prepare_synthetic_queries.py` | **생성** (stage 1) — 두 파일을 씀 |
| `scripts/build_synthetic_queries.py` | **소비** (stage 2) — mapping + `_synthetic_raw.json`을 읽어 평가셋 생성 |

### 10-2. ⚠️ 재현 좌표가 평가셋 본체에도 있습니다

지시대로 매핑을 함께 지웠지만, **`video_id` + `start_ms`/`end_ms` 좌표는
`data/eval/queries/youtube_synthetic_queries.jsonl`에도 51건 전부 들어 있습니다.**

```json
"video_id": "Ry3NnrbVjAM",
"relevant_spans": [{"span_id": "s001-s1", "start_ms": 221920, "end_ms": 276950,
                    "note": "생성 원본 chunk #6 (…)"}]
```

`_synthetic_mapping.json`과 기능적으로 같은 포인터입니다(`chunk_id`만 없음).
**그런데 이 파일은 평가셋 본체입니다** — 지우면 합성 질문 51건이 사라집니다.
자막 텍스트는 없고 좌표만 있습니다.

**판단하지 않았습니다.** 매핑을 지운 근거("원문보다 위험도가 높은 재현
레시피")를 그대로 적용하면 이 파일도 대상인데, 대가가 평가셋 소멸이라
성격이 다릅니다. 선택지는 세 가지로 보입니다 — 그대로 둔다 /
`relevant_spans`만 비운다(Hit@k 판정 불가) / `video_id`를 익명 ID로 치환한다.
결정해 주시면 처리하겠습니다.

### 10-3. HEAD의 `combined_v4_e5_metrics.json` — 텍스트 그대로 있음

| 항목 | 값 |
|---|---|
| blob 크기 | 419,786 bytes |
| `text` 필드 | **307개 / 116,681자** |
| 위치 | `owner_fixtures[].graph_top_k[]` 142, `owner_fixtures[].top_k[]` 100, `gold[].top_k[]` 60, `gold[].graph_top_k[]` 5 |

스키마 수정은 앞으로 생성될 파일에만 적용되므로 **커밋된 이 파일은 그대로**입니다.
참고로 추적되는 다른 `*_metrics.json` 6개의 최장 문자열은 87자이고, 480자
문자열을 가진 것은 이 파일뿐입니다.

### 10-4. 추가한 테스트 3개의 범위 — **"경로 하나"였습니다**

정확히는 그보다 좁았습니다. 세 테스트는 `without_chunk_text()`라는 **순수 함수를
단독으로** 검증합니다. 직렬화 경로가 그 함수를 호출하는지조차 확인하지 않습니다.
새 스크립트가 새 경로로 본문을 쓰면 세 테스트는 전부 통과한 채로 원문이
커밋됩니다. 6번을 그대로 수행했습니다.

## 11. 조치 — 자막 원문·매핑 제거 (4번)

`git rm` 했습니다.

- `data/eval/queries/_synthetic_prompt.md` (자막 지문 20개 6,671자)
- `data/eval/queries/_synthetic_mapping.json` (chunk_id·video_id·start_ms)

`.gitignore`의 `!data/eval/queries/**`를 **명시적 허용 목록으로 뒤집었습니다.**
디렉터리를 `**`로 여는 형태가 정확히 이번 사고의 형태이기 때문입니다.

`_synthetic_raw.json`은 남깁니다 — LLM이 반환한 질문 60개뿐이고 원문 지문도
좌표도 없으며, 합성 질문셋의 출처를 기록한 유일한 파일입니다.

**부작용**: 매핑이 없으면 clean clone에서 stage 2를 재실행할 수 없습니다.
다만 그 산출물(`youtube_synthetic_queries.jsonl` 51건)은 추적되므로 평가셋
자체는 영향받지 않습니다. 두 스크립트 독스트링에 사유를 적었습니다.

## 12. 불변식을 산출물 기준으로 (6번)

`tests/test_committed_artifacts_have_no_chunk_text.py` — **git이 추적하는 파일
전체**를 훑습니다. 어떤 스크립트가 무슨 경로로 썼는지와 무관하게 결과만 봅니다.

- `chunk_id`/`chunk_index`를 가진 객체에 150자 넘는 본문 필드가 있으면 실패.
- 자막 지문을 나르던 두 경로가 다시 추적되면 실패.
- 면제 목록(`KNOWN_LEGACY`)에는 `combined_v4_e5_metrics.json` 하나뿐이고,
  **그 파일이 더 이상 위반이 아니게 되면 목록이 낡았다고 실패**시킵니다.
  면제가 조용히 무의미해지지 않게 하는 장치입니다.

동작 확인: 면제를 해제하면 `combined_v4`를 잡고 실패합니다. 그 외 추적 파일에는
걸리는 것이 없습니다. 전체 284개 통과.

## 13. 제안 — 커밋된 스냅샷을 어떻게 할 것인가 (5번, 선택은 넘김)

**먼저 확인된 사실: 재생성은 선택지가 아닙니다.**

이 스냅샷의 `corpus.fingerprint`는 **문서 6건 / 57청크 · 영상 26청크** 시절
것입니다. 지금 코퍼스는 문서 17건 / 219청크입니다. 지금 다시 돌리면
"같은 스냅샷에서 본문만 뺀 것"이 아니라 **다른 숫자**가 나옵니다.
`docs/demo_scenarios.md`와 `reports/demo_outputs_0820.md`가 이 파일을
"2026-08-20 재측정 기준"으로 인용하고 있으므로, 숫자가 바뀌면 그 인용이
가리키는 대상이 사라집니다. 게다가 지금은 gold 확장이 진행 중이라 재실행 시점도
나쁩니다.

그래서 실질 선택지는 둘입니다.

| | A. 파일에서 `text`만 제거 | B. 파일 삭제 |
|---|---|---|
| Hit@1·MRR·score·rank | **전부 보존** | 소실 |
| 인용 2곳(demo_scenarios, demo_outputs) | 유효 | **깨짐** |
| 제거되는 것 | 본문 116,681자 | 파일 전체 |
| 실행 후 남는 것 | 식별자·점수·`where`·게이트 판정 | 없음 |
| 단점 | 실행 산출물과 byte-identical이 아니게 됨 | 실험 기록 소실 |

**A를 제안합니다.** 근거는 세 가지입니다.

1. **없애야 할 것과 지켜야 할 것이 파일 안에서 깔끔히 갈립니다.** 문제는 본문
   하나이고, 증거 가치는 전부 숫자에 있습니다. B는 둘 다 버립니다.
2. **"실행 산출물 그대로"라는 성질은 이미 이 파일의 성질이 아닙니다.**
   `DEFAULT_METRICS` 경로라 다음 전체 실행 때 어차피 덮어써집니다 — 그때는
   새 코드가 본문 없이 씁니다.
3. B의 유일한 이점은 "완전히 없앤다"인데, **이력에는 8개 버전이 그대로 남으므로
   그 이점이 성립하지 않습니다.**

**세 선택지 모두 이력을 바꾸지 않습니다.** A든 B든 HEAD만 달라지고, 과거 커밋의
116,681자는 `git show <sha>:…`로 그대로 나옵니다. 이건 이력 재작성으로만
바뀌고, 재작성은 하지 않기로 결정된 상태입니다. **즉 이 조치의 목적은
"이미 나간 것을 회수하는 것"이 아니라 "지금 clone하는 사람이 받는 파일에서
빼는 것"입니다.** 그 이상으로 읽지 마십시오.

**실행하지 않았습니다. A/B 지정해 주시면 처리하겠습니다.**

## 14. `.gitignore` 예외 재검토 (7번)

넓히는 방향은 제안하지 않았습니다. 결과는 좁히거나 유지입니다.

| 예외 | 판정 | 사유 |
|---|---|---|
| `!data/eval/queries/**` | **좁힘** | 명시적 허용 목록으로 교체. 이번 사고의 통로 |
| `!data/eval/results/*_metrics.json` | 유지 + **이중 가드** | 스냅샷은 정말로 재생성 불가. 대신 `without_chunk_text()` + 불변식 테스트로 신뢰를 걷어냄 |
| `!data/eval/results/*_report.md` | 유지, **아래 플래그** | 150자 스니펫을 설계상 찍음 |
| `!data/eval/generation/answers_*.jsonl` | 유지 | LLM 답변. 원자재와 40자 이상 일치 0건, 최장 문자열 444자(자체 생성문) |
| `!data/guardrail/*.json` | 유지 | 용어 사전. 원문 일치 0건 |
| `!data/profiles/demo_profile_v1.json` | 유지 | 사람이 쓴 픽스처 |
| `!data/reviews/*.csv` | 유지 | 사람의 검토 판정 |
| `!ui-experiments/**/*.html` | 유지 | 이 건과 무관 |

파일 머리에 **예외를 여는 규칙 3개**를 적었습니다 — `**`로 열지 않는다 /
신뢰가 아니라 테스트로 확인한다 / 넓히려면 무엇이 실릴 수 있는지 먼저 적는다.

### 남는 두 가지 — 판단 필요, 손대지 않음

1. **`reports/*.md`에는 gitignore 규칙이 아예 없습니다.** `reports/` 전체가
   추적되고, 평가 하네스가 150자 스니펫을 거기에 찍습니다
   (`combined_corpus_coverage.md` 4,524자 · `owner_fixtures_coverage.md`
   6,819자, 누적). 150자 발췌를 정책상 허용할지가 갈림길입니다. 금지로
   가면 `SNIPPET_CHARS`를 줄이거나 리포트에서 발췌를 빼야 합니다.
2. **`frozen/frozen_stage2_0820.jsonl`** — 추출 원본 문장 4,819자(최장 135자).
   `.gitignore`에 "여기서 의도적으로 제외하지 않는다"는 코멘트가 명시돼 있고,
   재추출로 재현 불가능하다는 사유가 붙어 있습니다. 그 사유는 여전히
   유효하지만 원문을 담고 있다는 점은 새 정책과 부딪힙니다.

## 15. 점검 중 발견 — 보고만, 조치 안 함

### 15-1. 제가 만든 노출을 고쳤습니다

`docs/SOURCES.md`와 `docs/document_ingest_design.md`에 wayopet 질문자의 부분
마스킹 실명을 **예시로 적었습니다**(`문*혜` 형태). 그 형태가 익명화가 아니라는
것을 지적하려고 쓴 문장인데, 지적하면서 같은 것을 한 번 더 게시한 셈입니다.
설명으로 바꿨습니다. **두 파일 모두 이미 push된 상태이므로 이력에는 남습니다.**

### 15-2. ⚠️ `origin/verify/document-parsing-829`이 24커밋 앞서 있고, wayopet이 인제스트됐습니다

푸시 보류 중 확인했습니다. 다른 세션이 이 브랜치에 계속 작업했습니다.

- `78159ae wayopet 6건 투입` — wayopet Q&A가 **코퍼스에 들어갔습니다.**
  제가 3절과 `docs/document_ingest_design.md`에 "여전히 미인제스트"로 적은 것은
  **그 시점 기준이었고 지금은 사실이 아닙니다.**
- `45c5f07`에서 제 브랜치를 머지해 `without_chunk_text()`는 origin tip에
  **들어가 있습니다.**
- 그런데 **스냅샷은 그 전에 재생성됐습니다.** origin tip의
  `combined_v4_e5_metrics.json`은 475,372 bytes · `text` 300개 · **105,326자**이고,
  그중 29개가 wayopet 유래입니다. **부분 마스킹 실명 `이*주`가 그 안에
  들어 있습니다.**

즉 **코드 가드는 있는데 산출물이 가드보다 먼저 만들어졌습니다.** 다음 전체
평가 실행 때 자동으로 본문 없이 다시 써집니다.

여기서 제 불변식 테스트의 한계가 드러납니다 — `KNOWN_LEGACY` 면제가
**경로 기준**이라, 같은 경로가 재생성되면서 내용이 늘어도 통과합니다.
13절의 A안(파일에서 `text` 제거)을 택하면 면제 항목 자체가 사라지므로 이 구멍도
같이 닫힙니다. **B안이나 보류를 택하면 이 구멍은 남습니다.**

**아무것도 머지하지 않았고 푸시하지 않았습니다.** 제 로컬 브랜치는
origin 대비 1 ahead / 24 behind입니다. 머지는 푸시 동결이 풀린 뒤에
결정하시는 것이 맞다고 봅니다 — 지금 머지하면 제 로컬에만 있는 삭제 커밋과
저쪽의 wayopet 산출물이 한 트리에서 만납니다.

### 15-3. 오탐으로 확인한 것

`reports/INDEX.md`·`reports/breed_conditionality_0822.md`의 `각*이`·`제*가`는
실명이 아니라 마크다운 강조(`*검색 축으로서의 기각*`)가 걸린 것입니다.
`scripts/import_gold_labels.py`의 `김*수`, `tests/test_split_wayopet_qa.py`의
`김*이`는 다른 세션이 만든 테스트 픽스처로 보이며 실존 인물 표기가 아닙니다.
다만 **후자 두 개는 제가 만들지 않은 파일이라 단정하지 않습니다.**
